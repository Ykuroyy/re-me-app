<div class="card">
    <h2>思い出の詳細</h2>
    <p style="text-align: center; color: #8b5a8c; margin-bottom: 20px;"><%= @good_memory.date.strftime("%Y年%m月%d日") %>の思い出</p>
    
    <div class="record">
        <div class="date"><%= @good_memory.date.strftime("%Y年%m月%d日") %></div>
        <div class="mood-display">
            <span class="mood-emoji"><%= @good_memory.mood_emoji %></span>
            <span class="mood-text"><%= @good_memory.mood_text %></span>
        </div>
        <div class="content"><%= @good_memory.content %></div>
    </div>
    
    <div style="text-align: center; margin: 20px 0;">
        <%= link_to "編集", edit_good_memory_path(@good_memory), class: "btn", style: "width: auto; margin: 0 10px;" %>
        <%= link_to "削除", good_memory_path(@good_memory), method: :delete, data: { confirm: "この思い出を削除しますか？" }, 
            class: "btn", style: "width: auto; margin: 0 10px; background: linear-gradient(135deg, #dc3545 0%, #c82333 100%);" %>
    </div>
</div>

<div class="nav-links">
    <%= link_to "← ホームに戻る", root_path %>
</div>
