<div class="space-y-8">
  <!-- ヘッダー -->
  <div class="bg-white rounded-2xl shadow-lg p-8 border-4 border-pink-100">
    <div class="flex justify-between items-center">
      <div>
        <h1 class="text-3xl font-bold text-pink-600 mb-2"><%= @month_name %>の思い出</h1>
        <p class="text-gray-600">この月に記録した良かったことを振り返ってみましょう</p>
      </div>
      <div class="flex space-x-3">
        <%= link_to "← ホームに戻る", root_path, class: "text-pink-600 hover:text-pink-800 font-semibold" %>
        <%= link_to "新しい思い出", new_good_memory_path, class: "bg-pink-500 hover:bg-pink-600 text-white px-4 py-2 rounded-lg transition-colors" %>
      </div>
    </div>
  </div>

  <!-- 月別統計 -->
  <div class="grid grid-cols-1 md:grid-cols-4 gap-6">
    <div class="bg-white rounded-xl shadow-lg p-6 border-2 border-pink-100 text-center">
      <div class="text-3xl font-bold text-pink-600 mb-2"><%= @good_memories.count %></div>
      <div class="text-gray-600">記録数</div>
    </div>
    <div class="bg-white rounded-xl shadow-lg p-6 border-2 border-pink-100 text-center">
      <div class="text-3xl font-bold text-purple-600 mb-2">
        <%= @good_memories.where(mood: ['good', 'very_good']).count %>
      </div>
      <div class="text-gray-600">良い気分の日</div>
    </div>
    <div class="bg-white rounded-xl shadow-lg p-6 border-2 border-pink-100 text-center">
      <div class="text-3xl font-bold text-green-600 mb-2">
        <%= @good_memories.where(mood: 'very_good').count %>
      </div>
      <div class="text-gray-600">とても良い日</div>
    </div>
    <div class="bg-white rounded-xl shadow-lg p-6 border-2 border-pink-100 text-center">
      <div class="text-3xl font-bold text-blue-600 mb-2">
        <%= @good_memories.where(mood: ['very_bad', 'bad']).count %>
      </div>
      <div class="text-gray-600">辛い日</div>
    </div>
  </div>

  <!-- 思い出一覧 -->
  <div class="bg-white rounded-2xl shadow-lg p-8 border-4 border-pink-100">
    <% if @good_memories.any? %>
      <div class="space-y-6">
        <% @good_memories.each do |memory| %>
          <div class="border-l-4 border-pink-300 pl-6 py-4 bg-gradient-to-r from-pink-50 to-white rounded-r-lg">
            <div class="flex justify-between items-start mb-2">
              <div class="flex items-center space-x-3">
                <span class="text-2xl"><%= memory.mood_emoji %></span>
                <span class="text-sm text-gray-500"><%= memory.date.strftime("%m月%d日") %></span>
                <span class="text-sm bg-pink-100 text-pink-700 px-2 py-1 rounded-full"><%= memory.mood_text %></span>
              </div>
              <div class="flex space-x-2">
                <%= link_to "詳細", memory, class: "text-pink-600 hover:text-pink-800 text-sm" %>
                <%= link_to "編集", edit_good_memory_path(memory), class: "text-blue-600 hover:text-blue-800 text-sm" %>
              </div>
            </div>
            <p class="text-gray-700 leading-relaxed"><%= memory.content %></p>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="text-center py-12">
        <div class="text-6xl mb-4">📅</div>
        <p class="text-gray-600 text-lg mb-4"><%= @month_name %>の思い出はありません</p>
        <p class="text-gray-500">この月の良かったことを記録してみませんか？</p>
        <div class="mt-6">
          <%= link_to "新しい思い出を記録", new_good_memory_path, class: "bg-pink-500 hover:bg-pink-600 text-white px-6 py-3 rounded-lg font-semibold transition-colors" %>
        </div>
      </div>
    <% end %>
  </div>

  <!-- 月別ナビゲーション -->
  <div class="bg-white rounded-2xl shadow-lg p-8 border-4 border-pink-100">
    <h2 class="text-2xl font-bold text-gray-800 mb-6 flex items-center">
      <span class="text-purple-500 mr-2">📅</span>
      他の月を見る
    </h2>
    <div class="grid grid-cols-2 md:grid-cols-6 gap-4">
      <% (0..11).each do |i| %>
        <% date = i.months.ago %>
        <%= link_to monthly_memories_path(date.year, date.month), 
            class: "block p-4 text-center bg-gradient-to-br from-purple-50 to-pink-50 rounded-lg border-2 border-purple-100 hover:border-purple-300 transition-colors #{'border-purple-500 bg-purple-100' if date.year == @year && date.month == @month}" do %>
          <div class="text-lg font-semibold text-purple-700"><%= date.strftime("%m月") %></div>
          <div class="text-sm text-gray-600"><%= date.strftime("%Y年") %></div>
        <% end %>
      <% end %>
    </div>
  </div>

  <!-- 励ましメッセージ -->
  <div class="bg-gradient-to-r from-yellow-50 to-orange-50 rounded-xl p-6 border-2 border-yellow-200">
    <div class="text-center">
      <div class="text-3xl mb-3">🌟</div>
      <h3 class="text-lg font-semibold text-gray-800 mb-2">素敵な思い出がたくさんありますね</h3>
      <p class="text-gray-600">毎日の小さな幸せを大切にすることで、心が豊かになります。これからも素敵な思い出を作っていきましょう！</p>
    </div>
  </div>
</div> 